# 프로젝트 평가 리포트 — 2026.02.22

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | flow-front |
| 언어/런타임 | TypeScript 5.9 / Browser (ES2022, Vite 7 번들) |
| 소스 파일 수 | 100개 (.ts/.tsx) |
| 테스트 파일 수 | — (미구성) |
| 총 코드 라인 | ~10,931줄 |
| 빌드 결과물 | JS 1,023 kB / CSS 74.7 kB (gzipped: JS 315 kB / CSS 13 kB) |
| 아키텍처 | React 19 + TanStack Query + React Hook Form + @xyflow/react |

**요약**: 워크플로우 관리 대시보드 프론트엔드. 14개 페이지, 56개 컴포넌트(UI 13 + 커스텀 43), 8개 React Query 훅, 9개 API 모듈로 구성. TypeScript 타입 체크·빌드 통과, ESLint 경고 3건(에러 0). `any` 타입 사용 0건. 인증(auth), 워크스페이스(workspace), 워크플로우 빌더 등 풍부한 기능을 갖춘 성숙한 SPA.

---

## 2. 아키텍처 평가

### 2.1 디렉토리 구조

```
src/
├── pages/         (14 files)  — 페이지 컴포넌트 (라우트 단위)
├── components/
│   ├── ui/        (13 files)  — shadcn/ui 기본 컴포넌트
│   ├── layout/    (3 files)   — AppLayout, Sidebar, PageHeader
│   ├── flow/      (20 files)  — @xyflow 노드/엣지/패널
│   ├── workspace/ (3 files)   — 워크스페이스 전용 컴포넌트
│   ├── auth/      (1 file)    — ProtectedRoute
│   └── common     (9 files)   — ConfirmDialog, StatusBadge, DataTable 등
├── hooks/         (8 files)   — React Query 커스텀 훅
├── api/           (9 files)   — API 클라이언트, 타입, 리소스별 함수
├── lib/           (4 files)   — utils, constants, format, query-keys
└── assets/
```

| 영역 | 파일 수 | 주요 내용 |
|------|:-------:|----------|
| 페이지 | 14 | Dashboard, WorkflowList, WorkflowBuilder, WorkflowRunFlow, Workspace, GitManagement, McpServer, Login 등 |
| 컴포넌트 (UI) | 13 | button, input, dialog, select, table, badge, card, tabs 등 (shadcn) |
| 컴포넌트 (커스텀) | 43 | Flow 노드/엣지/패널 20개, 레이아웃 3개, 워크스페이스 3개, 공통 9개, auth 1개 |
| 훅 | 8 | useWorkflows, useWorkflowRuns, useGits, useMcpServers, useAgentLogs, useAuth, useWorkspaces, useWorkLineage |
| API | 9 | client, types, workflows, workflow-runs, gits, mcp-servers, auth, workspaces, work-lineage |
| 유틸리티 | 4 | utils(cn), constants, format, query-keys |

**평가: A**
- 명확한 관심사 분리: pages → hooks → api → lib 계층 구조
- shadcn/ui 컴포넌트를 별도 분리하여 커스텀 수정 최소화
- Flow(xyflow) 관련 컴포넌트가 `components/flow/`에 잘 격리됨

### 2.2 API 레이어 격리

| 검사 항목 | 결과 |
|-----------|------|
| 컴포넌트에서 Axios 직접 import | **0건** ✅ |
| API 타입 중앙 관리 (`api/types.ts`) | ✅ |
| 리소스별 API 객체 분리 | ✅ (7개 리소스) |
| apiClient response interceptor | ✅ (`response.data` 자동 추출) |

**평가: A+**
- 모든 HTTP 호출이 `api/` 모듈을 통해 격리됨
- 컴포넌트에서 Axios 직접 사용 0건

### 2.3 React Query 커스텀 훅 패턴

| 훅 | Query | Mutation | invalidateQueries |
|----|:-----:|:--------:|:-----------------:|
| useWorkflows | ✅ list, get | ✅ create, update, delete | ✅ |
| useWorkflowRuns | ✅ list, get, summary | ✅ cancel, pause, resume, restore, start | ✅ |
| useGits | ✅ list | ✅ create, delete | ✅ |
| useMcpServers | ✅ list | ✅ register, unregister | ✅ |
| useAgentLogs | ✅ list | — | — |
| useAuth | — | ✅ login, register, logout | ✅ |
| useWorkspaces | ✅ list, get | ✅ create, delete, complete, sendChat, push, merge, buildWorkflow | ✅ |
| useWorkLineage | ✅ lineage | — | — |

**평가: B+**
- 8개 커스텀 훅이 대부분의 서버 상태를 관리
- **위반 4건**: 일부 페이지/패널에서 `useQuery`를 직접 사용 (아래 상세 분석 참조)

### 2.4 컴포넌트 작성 패턴

| 검사 항목 | 결과 |
|-----------|------|
| `function` 선언 + named export | **14/14 페이지** ✅ |
| `export default` 위반 | **1건** (`api/client.ts`) |
| Props interface 정의 | 대부분 준수 ✅ |

**평가: A**
- 모든 페이지가 `export function XxxPage()` 패턴 준수
- `client.ts`의 default export는 경미한 위반

### 2.5 스타일링 & 상수 관리

| 검사 항목 | 결과 |
|-----------|------|
| `cn()` 유틸 사용 | ✅ |
| `constants.ts` 상수 맵 | ✅ (RUN_STATUS_COLOR 등 정의) |
| 매직 스트링 위반 | **6개+ 파일** ⚠️ |

**평가: B**
- `constants.ts`에 주요 상수가 정의되어 있으나, 일부 페이지에서 인라인 상태 비교·색상 하드코딩이 잔존

---

## 3. 코드 품질 검증

### 3.1 TypeScript 타입 체크

```
$ npx tsc -b
→ ✅ PASS (0 errors)
```

strict 모드에서 에러 없음.

### 3.2 ESLint 검사

```
$ npm run lint
→ ⚠️ 0 errors, 3 warnings
```

| 파일 | 규칙 | 심각도 | 내용 |
|------|------|:------:|------|
| `components/flow/panels/RuntimeWorkNodePanel.tsx:144` | react-hooks/incompatible-library | Warning | `form.watch()` 메모이제이션 불가 |
| `pages/McpServerManagementPage.tsx:73` | react-hooks/incompatible-library | Warning | `form.watch()` 메모이제이션 불가 |
| `pages/WorkflowListPage.tsx:48` | react-hooks/exhaustive-deps | Warning | `data` 논리 표현식이 useMemo deps 변경 유발 |

3개 모두 React Hook Form의 `watch()` API와 React Compiler 호환성 관련 경고. 기능상 문제는 없으나 React Compiler 도입 시 주의 필요.

### 3.3 빌드 검증

```
$ npm run build (tsc -b && vite build)
→ ✅ PASS
   2,432 modules transformed
   JS: 1,023.61 kB (gzip: 315.20 kB)
   CSS: 74.68 kB (gzip: 13.14 kB)
```

| 항목 | 수치 | 비고 |
|------|------|------|
| 번들 JS 크기 | 1,023 kB | ⚠️ 500 kB 경고 임계값 초과 |
| 번들 CSS 크기 | 74.7 kB | 양호 |
| Gzip JS | 315 kB | 수용 가능 |
| 모듈 수 | 2,432 | — |

빌드 성공하나, JS 번들이 1 MB를 초과하여 Vite의 chunk size 경고 발생. 코드 스플리팅 필요.

---

## 4. 상세 문제 분석

### 🟡 Warning — 페이지/패널에서 useQuery 직접 사용 (4건)

CLAUDE.md 규칙: "페이지 컴포넌트에서 직접 `useQuery`/`useMutation`을 사용하지 않는다"

| 파일 | 내용 |
|------|------|
| `pages/WorkspaceDetailPage.tsx` | `useQuery`로 파일 내용 직접 조회 |
| `pages/WorkflowListPage.tsx` | `useQueryClient` import + 직접 `Promise.all()` 삭제 |
| `components/flow/panels/WorkspaceViewerPanel.tsx` | `useQuery` 2회 직접 사용 (tree, file) |
| `components/flow/panels/ReportViewerPanel.tsx` | `useQuery` 직접 사용 + 하드코딩 queryKey |

### 🟡 Warning — 매직 스트링 인라인 사용 (6개+ 파일)

`constants.ts`에 상수가 정의되어 있으나 일부 파일에서 미참조:

| 파일 | 내용 |
|------|------|
| `pages/DashboardPage.tsx` | 인라인 색상 (`text-green-600`, `text-yellow-600` 등) |
| `pages/WorkflowListPage.tsx` | 인라인 상태 비교 문자열 |
| `pages/WorkflowRunFlowPage.tsx` | 인라인 상태 비교 |
| `pages/WorkspaceDetailPage.tsx` | 인라인 상태 비교 |
| `pages/McpServerManagementPage.tsx` | 인라인 `transportColor` 맵 (→ constants.ts로 이동 필요) |
| `pages/WorkflowFlowEditorPage.tsx` | 인라인 상태 비교 |

### 🟡 Warning — 번들 크기 초과

JS 번들이 1,023 kB로 Vite 기본 임계값(500 kB)을 2배 초과. `@xyflow/react` 등 대형 라이브러리의 영향으로 추정. 동적 import 기반 코드 스플리팅 필요.

### 🟢 Info — `api/client.ts`의 default export

`export default apiClient` 사용. CLAUDE.md 규칙상 `App.tsx`만 default export 허용. 경미한 위반이나 API 레이어 내부 사용이므로 영향 적음.

### 🟢 Info — ESLint 경고 3건 (react-hooks 관련)

React Hook Form의 `watch()` API가 React Compiler와 호환되지 않는다는 경고. 현재 기능 동작에는 영향 없으나, React Compiler 정식 도입 시 대응 필요.

---

## 5. 강점

### 5.1 타입 안전성 100%

소스 코드 전체에서 `any` 타입 사용 **0건** (`as any` 0건, `: any` 0건). TypeScript strict 모드 완전 통과. 백엔드 DTO와 `api/types.ts`의 타입 동기화가 잘 유지됨.

### 5.2 API 레이어 완벽 격리

컴포넌트에서 Axios 직접 사용 **0건**. 모든 HTTP 호출이 `api/{resource}.ts` → `hooks/use{Resource}.ts` → 페이지 컴포넌트로 단방향 흐름을 따름.

### 5.3 일관된 컴포넌트 패턴

14개 페이지 컴포넌트가 100% `function` 선언 + named export 패턴을 준수. Props interface도 컴포넌트 위에 일관되게 정의.

### 5.4 풍부한 기능 완성도

100개 소스 파일로 다음 기능을 완전히 구현:
- 대시보드 (실행 현황 요약)
- 워크플로우 CRUD + 빌더 (드래그&드롭 플로우 에디터)
- 워크플로우 실행 모니터링 (실시간 SSE)
- Git/MCP 서버 관리
- 에이전트 로그 뷰어
- 워크스페이스 (채팅, 파일 탐색, Diff, 브랜치 Push/Merge)
- 인증 (로그인/회원가입, ProtectedRoute)
- Work Lineage 추적

### 5.5 React Query 키 팩토리

`lib/query-keys.ts`에서 모든 쿼리 키를 계층적으로 관리. `invalidateQueries` 시 정확한 범위 지정 가능.

### 5.6 shadcn/ui 분리

UI 기본 컴포넌트 13개를 `components/ui/`에 격리하여 직접 수정을 최소화. 커스터마이징 시 래핑 패턴 사용.

---

## 6. 종합 평가

| 영역 | 점수 | 비고 |
|------|:----:|------|
| **디렉토리 구조** | A | 명확한 관심사 분리, flow 컴포넌트 격리 |
| **API 레이어** | A+ | Axios 직접 사용 0건, 타입 중앙 관리 |
| **상태 관리** | B+ | 대부분 훅 패턴 준수, 직접 useQuery 4건 위반 |
| **타입 안전성** | A+ | `any` 0건, strict 모드 통과 |
| **코드 품질** | A- | ESLint 에러 0건, 경고 3건 |
| **컴포넌트 패턴** | A | function + named export 100%, default export 1건 |
| **상수/스타일 관리** | B | constants.ts 존재하나 인라인 매직 스트링 6건+ |
| **빌드/번들** | B- | 빌드 성공, 번들 1 MB 초과 (코드 스플리팅 필요) |
| **테스트** | F | 프론트엔드 테스트 미구성 |
| **기능 완성도** | A | 14페이지, 56컴포넌트, 8훅 — 풍부한 기능 |

### 종합 등급: **B+**

타입 안전성과 API 레이어 격리는 모범적이며, 컴포넌트 패턴도 일관성 있게 유지. 다만 일부 페이지에서 React Query 직접 사용, 매직 스트링 잔존, 번들 크기 초과, 프론트엔드 테스트 부재가 등급을 제한하는 요인.

---

## 7. 이전 리포트와 비교

프론트엔드 첫 번째 평가이므로 비교 대상 없음. 향후 리포트에서 추적할 기준선:

| 지표 | 001 (현재) | 비고 |
|------|:----------:|------|
| 소스 파일 수 | 100개 | .ts + .tsx |
| 총 코드 라인 | ~10,931줄 | — |
| 페이지 수 | 14 | — |
| 컴포넌트 수 | 56 (UI 13 + 커스텀 43) | — |
| 커스텀 훅 수 | 8 | — |
| API 모듈 수 | 9 | — |
| 타입 체크 | ✅ PASS | — |
| ESLint | 0 errors, 3 warnings | — |
| 빌드 | ✅ PASS | JS 1,023 kB |
| `any` 사용 | 0건 | — |
| 직접 useQuery 위반 | 4건 | — |
| 매직 스트링 위반 | 6건+ | — |
| 번들 크기 | 1,023 kB (gzip 315 kB) | 500 kB 경고 초과 |
| 종합 등급 | **B+** | — |

---

## 8. 우선순위별 개선 사항

### 🔴 즉시 수정 (P0)

없음 — 타입 체크·빌드·기능 동작에 치명적 문제 없음.

### 🟡 단기 개선 (P1)

1. **직접 useQuery/useMutation 제거**
   - `WorkspaceDetailPage.tsx`: 파일 조회를 `useWorkspaces` 훅에 추가
   - `WorkspaceViewerPanel.tsx`: tree/file 조회를 커스텀 훅으로 이동
   - `ReportViewerPanel.tsx`: 리포트 조회를 `useWorkflowRuns` 훅에 추가, queryKey를 `queryKeys` 팩토리로 변경
   - `WorkflowListPage.tsx`: bulk delete를 mutation으로 래핑

2. **매직 스트링 상수화**
   - `McpServerManagementPage.tsx`의 `transportColor` → `constants.ts`의 `MCP_TRANSPORT_COLOR`
   - `DashboardPage.tsx`의 인라인 색상 → `constants.ts` 참조
   - 상태 비교 문자열을 enum 또는 상수로 관리

3. **번들 크기 최적화**
   - React Router 기반 lazy loading (코드 스플리팅)
   - `@xyflow/react` 관련 페이지를 동적 import
   - `build.rollupOptions.output.manualChunks` 설정

### 🟢 중기 개선 (P2)

4. **프론트엔드 테스트 도입**
   - Vitest + React Testing Library 설정
   - 커스텀 훅 단위 테스트 (useWorkflows, useAuth 등)
   - 핵심 페이지 통합 테스트 (WorkflowListPage, DashboardPage)

5. **ESLint 경고 해소**
   - `react-hooks/incompatible-library`: React Hook Form `watch()` 대안 패턴 적용 (useWatch 등)
   - `react-hooks/exhaustive-deps`: useMemo 의존성 정리

6. **`api/client.ts` named export 전환**
   - `export default apiClient` → `export const apiClient`
   - import하는 파일들 일괄 수정
