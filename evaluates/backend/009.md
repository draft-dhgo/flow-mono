# 프로젝트 평가 리포트 — 2026.02.22

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | workflow-backend |
| 언어/런타임 | TypeScript 5.9.3 / Node.js (ES2022, ESM) |
| 소스 파일 수 | 404개 (.ts) |
| 테스트 파일 수 | 66개 |
| 소스 코드 라인 | ~18,519줄 |
| 테스트 코드 라인 | ~7,867줄 |
| 외부 런타임 의존성 | @anthropic-ai/claude-agent-sdk, @nestjs/common, @nestjs/core, @nestjs/platform-express, @nestjs/typeorm, class-transformer, class-validator, bcrypt, pg, reflect-metadata, rxjs, typeorm, uuid |
| 아키텍처 | Hexagonal Architecture + DDD + Vertical Slice |

**요약**: AI 에이전트 기반 워크플로우 실행 관리 백엔드. 008 대비 소스 코드 +3,418줄(+23%), 소스 파일 +52개(+15%) 대폭 증가. 새로운 `workspace` 모듈(32파일, 1,663줄)이 추가되었고, TCC 패턴·Transactional Outbox·DLQ·Optimistic Locking 등 인프라 기반이 대거 확충됨. 7개 Bounded Context로 확장. TypeScript 타입 체크, ESLint, 테스트(370/370) 모두 클린 통과 유지.

---

## 2. 아키텍처 평가

### 2.1 Vertical Slice 구조

```
src/
├── common/           (72 files, ~2,773 lines)  — 공유 ID, 이벤트, 포트, 에러, UoW, Outbox, DLQ
├── workflow/         (45 files, ~2,352 lines)  — 워크플로우 정의/관리
├── workflow-runtime/ (106 files, ~6,402 lines) — 워크플로우 실행/런타임
├── git/              (41 files, ~1,582 lines)  — Git 저장소 관리
├── mcp/              (36 files, ~966 lines)    — MCP 서버 관리
├── agent/            (42 files, ~1,545 lines)  — AI 에이전트 세션 관리
├── auth/             (20 files, ~509 lines)    — 사용자 인증
├── workspace/        (32 files, ~1,663 lines)  — 워크스페이스 관리 (NEW)
├── mcp-gateway/      (8 files, ~594 lines)     — MCP Gateway (오케스트레이션)
├── app.module.ts                               — NestJS 루트 모듈
└── main.ts                                     — 애플리케이션 엔트리포인트
```

| Feature | domain/ | application/ | infra/ | presentation/ | 상태 |
|---------|:---:|:---:|:---:|:---:|:---:|
| workflow | ✅ | ✅ | ✅ | ✅ | 완전 |
| workflow-runtime | ✅ | ✅ | ✅ | ✅ | 완전 |
| git | ✅ | ✅ | ✅ | ✅ | 완전 |
| mcp | ✅ | ✅ | ✅ | ✅ | 완전 |
| agent | ✅ | ✅ | ✅ | ✅ | 완전 |
| auth | ✅ | ✅ | ✅ | ✅ | 완전 |
| workspace | ✅ | ✅ | ✅ | ✅ | 완전 (NEW) |
| mcp-gateway | — | ✅ | — | ✅ | 오케스트레이션 레이어 |

**평가: A+ (이전 A+ → 유지)**
- 7개 Bounded Context가 완전한 수직 슬라이스 유지
- 새로운 `workspace` 모듈이 기존 아키텍처 규칙을 완벽히 준수 (Workspace Aggregate Root, WorkspaceRepository 포트, In-Memory 어댑터, Controller, Module)
- `common/` 모듈이 TCC·UoW·Outbox·DLQ 인프라로 868줄 증가 — 트랜잭션 기반 이벤트 발행 인프라 완비

### 2.2 DDD 적용 수준

| DDD 개념 | 적용 | 상세 |
|-----------|:---:|------|
| Aggregate Root | ✅ | 12개 — Workflow, WorkflowRun, Git, McpServer, AgentSession, Report, WorkExecution, Checkpoint, WorkTree, WorkflowSpace, User, **Workspace(NEW)** |
| Value Object | ✅ | Branded Type + 팩토리 패턴 30종 이상 (IssueKey, GitUrl, BranchStrategy, CommitHash, UserId, **WorkspaceId, WorkspaceStatus, WorkspacePurpose(NEW)** 등) |
| Domain Event | ✅ | 31종 이벤트 (**WorkspaceCreated, WorkspaceCompleted(NEW)**), `BaseDomainEvent` 상속, `Object.freeze()` 페이로드 |
| Repository Port | ✅ | 13개 Repository 포트 (**WorkspaceRepository(NEW)**) — 모두 추상 클래스, In-Memory + TypeORM 이중 어댑터 |
| Bounded Context | ✅ | 7개 컨텍스트 명확 분리, `mcp-gateway`는 Facade 포트 기반 |
| Invariant 보호 | ✅ | private 생성자 + `create()`/`fromProps()` 팩토리 + 상태 전이 가드 |
| State Machine | ✅ | 엔티티별 명시적 상태 전이, `InvalidStateTransitionError`로 차단 |
| Error Hierarchy | ✅ | DomainError(isTransient) → ApplicationError 2단계 계층 |

**평가: A+ (이전 A+ → 유지)**
- 12개 Aggregate Root가 DDD 패턴 100% 준수
- `workspace` 도메인이 Workspace 엔티티, WorkspaceId Branded Type, WorkspaceStatus/WorkspacePurpose VO 등 DDD 패턴을 올바르게 적용
- 도메인 이벤트 2종 추가 (29 → 31)

### 2.3 헥사고날 아키텍처

| 모듈 | Port (도메인) | In-Memory Adapter | TypeORM Adapter | Controller | Module |
|------|:---:|:---:|:---:|:---:|:---:|
| workflow | ✅ | ✅ | ✅ | ✅ | ✅ |
| workflow-runtime | ✅ (7개+) | ✅ (7개+) | ✅ | ✅ | ✅ |
| git | ✅ | ✅ | ✅ | ✅ | ✅ |
| mcp | ✅ | ✅ | ✅ | ✅ | ✅ |
| agent | ✅ | ✅ | ✅ | ✅ | ✅ |
| auth | ✅ | ✅ | ✅ | ✅ | ✅ |
| workspace | ✅ | ✅ | — | ✅ | ✅ (NEW) |
| common | ✅ (공유 포트 8개+, Facade 4개+) | ✅ | ✅ | — | ✅ |
| mcp-gateway | — | — | — | ✅ | ✅ |

**평가: A (이전 A+ → A)**
- `workspace` 모듈이 In-Memory 어댑터만 구현, TypeORM 어댑터가 아직 없음
- 다른 모든 모듈은 Port → In-Memory → TypeORM 3중 어댑터 패턴 유지

### 2.4 TCC + UnitOfWork + Transactional Outbox (NEW)

008 대비 가장 큰 아키텍처 변화. 트랜잭션 안전성을 위한 인프라가 대거 추가됨:

| 인프라 | InMemory | TypeORM | 상태 |
|--------|:---:|:---:|:---:|
| UnitOfWork | ✅ `InMemoryUnitOfWork` | ✅ `TypeOrmUnitOfWork` | 완전 |
| CompensationStack | ✅ (TCC Cancel 단계) | ✅ | 완전 |
| Outbox | ✅ `InMemoryOutbox` | ✅ `outbox_messages` 스키마 | 완전 |
| OutboxRelay | ✅ 5초 폴링, 재시도 | ✅ | 완전 |
| Dead Letter Queue | ✅ `InMemoryDeadLetterQueue` | ✅ `dead_letter_messages` 스키마 | 완전 |
| OptimisticLock | — | ✅ version 기반 | 완전 |

**평가: A+**
- workflow-runtime Command Use Case들이 TCC 패턴 적용 완료
- `unitOfWork.run()` 내에서 save + event publish 원자적 커밋
- CompensationStack으로 LIFO 롤백

---

## 3. 코드 품질 검증

### 3.1 TypeScript 타입 체크

```
$ npm run typecheck
→ ✅ PASS (0 errors)
```

strict 모드에서 에러 없음. 4회 연속 클린 통과.

### 3.2 ESLint 검사

```
$ npm run lint
→ ✅ PASS (0 errors, 0 warnings)
```

4회 연속 클린 통과 유지.

### 3.3 테스트 실행

```
$ npm run test
→ ✅ 66 test files passed (66 files)
   370 tests passed (370 tests)
   Duration: 9.56s
```

| 테스트 계층 | 파일 수 | 통과/전체 | 상태 |
|-------------|:-------:|:---------:|:---:|
| 아키텍처 검증 | 1 | 11/11 | ✅ |
| 단위 - 도메인 | 13 | ~130/~130 | ✅ |
| 단위 - 애플리케이션 | 46 | ~160/~160 | ✅ |
| 단위 - 인프라 | 2 | 34/34 | ✅ |
| 통합 | 4 | ~19/~19 | ✅ |

테스트 케이스 수는 008과 동일(370/370)하나, workspace 모듈에 대한 테스트가 아직 없음.

---

## 4. 상세 문제 분석

### 🟡 Warning — workspace 모듈 테스트 부재

`workspace` 모듈은 32파일/1,663줄로 상당한 규모이나, 도메인 단위·애플리케이션 단위 테스트가 아직 작성되지 않았다. 8개 Command Use Case와 6개 Query, Workspace Aggregate Root에 대한 테스트가 필요하다.

### 🟡 Warning — workspace 모듈 TypeORM 어댑터 미구현

다른 모든 모듈이 In-Memory + TypeORM 이중 어댑터를 갖추고 있으나, workspace 모듈은 `InMemoryWorkspaceRepository`만 존재한다. DB 모드(`USE_DB=true`)에서 workspace 기능이 동작하지 않음.

### 🟢 Info — 테스트 파일명 컨벤션 혼재 (지속)

| 컨벤션 | 파일 수 | 비율 | 예시 |
|--------|:-------:|:----:|------|
| PascalCase | 18 | 27% | `AgentSession.test.ts`, `Workflow.test.ts` |
| kebab-case | 48 | 73% | `create-git-use-case.test.ts`, `git.test.ts` |

003 리포트부터 지적된 동일 이슈. 008과 동일 비율.

### 🟢 Info — mcp-gateway의 domain/infra 레이어 미존재 (지속)

`mcp-gateway`는 오케스트레이션 레이어로, `domain/`과 `infra/` 디렉토리가 없다. 의도된 설계.

---

## 5. 강점

### 5.1 workspace 모듈의 아키텍처 일관성

새로 추가된 `workspace` 모듈(32파일, 1,663줄)이 기존 아키텍처 패턴을 충실히 준수:
- Workspace Aggregate Root: `AggregateRoot<WorkspaceId>` 상속, private 생성자, create/fromProps 팩토리
- Value Objects: WorkspaceStatus, WorkspacePurpose, WorkspaceGitRef
- WorkspaceRepository: 추상 클래스 포트 + In-Memory 구현체
- 도메인 에러: WorkspaceDomainError 계층 (Invariant, InvalidStateTransition, EntityNotFound)
- 도메인 이벤트: WorkspaceCreated, WorkspaceCompleted
- CQRS: 8개 Command + 6개 Query 분리

### 5.2 TCC + Outbox 인프라 완성

`common/` 모듈에 트랜잭션 안전성 인프라가 체계적으로 구축됨:
- `UnitOfWork` 포트 + InMemory/TypeORM 구현체
- `CompensationStack`으로 TCC Cancel 단계 자동 롤백
- `TransactionalEventPublisher`로 Outbox 기록 + 즉시 발행
- `OutboxRelay`로 미발행 메시지 5초 주기 재전송 (최대 5회)
- `DeadLetterQueue`로 실패 메시지 격리
- `OptimisticLockError`로 동시성 충돌 감지

### 5.3 도메인 레이어 순수성 완전 유지

모든 feature의 도메인 레이어(`**/domain/**/*.ts`)에서:
- NestJS import: **0건**
- `@Injectable` 데코레이터: **0건**
- `as any`: **0건**
- 다른 feature 직접 import: **0건**

### 5.4 타입 안전성 100% 유지

소스 코드 전체에서 `as any` 사용 **0건**, `: any` 사용 **0건**. TypeScript strict 모드 완전 통과. Branded Type 30종 이상.

### 5.5 CQRS 패턴 엄격 준수

- Command Use Case: EventPublisher + UnitOfWork 주입, TCC 패턴으로 상태 변경
- Query Use Case: EventPublisher 미주입, 읽기 전용 작업만 수행
- 위반 사례 **0건**

### 5.6 3개 검증 도구 4회 연속 클린 통과

TypeScript 타입 체크, ESLint, Vitest 테스트가 모두 에러/경고 0건으로 통과. 4회 연속 클린 상태.

### 5.7 TODO/FIXME/HACK 0건

소스 코드 전체에서 기술적 부채 마커가 **0건**.

---

## 6. 종합 평가

| 영역 | 점수 | 이전(008) | 변화 | 비고 |
|------|:----:|:---------:|:----:|------|
| **아키텍처 설계** | A+ | A+ | → | workspace 모듈 추가 + TCC/Outbox 인프라 |
| **도메인 모델링** | A+ | A+ | → | 12 AR, 31 Event, 13 Repo Port, 30+ VO |
| **타입 안전성** | A+ | A+ | → | `as any` 0건, `: any` 0건 유지 |
| **코드 품질** | A+ | A+ | → | ESLint 0건, TODO/FIXME 0건 |
| **테스트** | A- | A+ | ↓ | 370/370 통과이나 workspace 테스트 부재 |
| **완성도** | A | A+ | ↓ | workspace TypeORM 미구현, 테스트 미작성 |
| **코드 컨벤션** | A- | A- | → | 테스트 파일명 혼재 잔존 |

### 종합 등급: **A+**

008과 동일 등급 유지하되, workspace 모듈의 테스트·TypeORM 부재로 완성도 영역이 소폭 하락. 주요 변화:

1. **workspace 모듈 신규 추가**: 32파일/1,663줄, DDD + 헥사고날 패턴 준수 (테스트·TypeORM은 미완)
2. **TCC + Outbox 인프라 완성**: UnitOfWork, CompensationStack, TransactionalEventPublisher, OutboxRelay, DLQ
3. **common 모듈 대폭 확장**: 1,905줄 → 2,773줄 (+868줄, +46%)
4. **코드량 23% 증가**: 인프라 성숙도가 크게 향상된 반면 테스트는 정체

---

## 7. 이전 리포트와 비교

| 지표 | 008 (이전) | 009 (현재) | 변화 |
|------|:----------:|:----------:|:----:|
| 소스 파일 수 | 352개 | 404개 | +52 (+15%) |
| 테스트 파일 수 | 66개 | 66개 | ±0 |
| 소스 코드 라인 | ~15,101줄 | ~18,519줄 | +3,418 (+23%) |
| 테스트 코드 라인 | ~7,831줄 | ~7,867줄 | +36 (+0.5%) |
| 테스트 케이스 수 | 370개 | 370개 | ±0 |
| 테스트/소스 비율 | 51.9% | 42.5% | -9.4% ↓ |
| 타입 체크 | ✅ PASS | ✅ PASS | — |
| ESLint | 0 errors | 0 errors | — |
| 테스트 결과 | 370/370 통과 | 370/370 통과 | — |
| `as any` 사용 | 0건 | 0건 | — |
| 도메인 간 위반 | 0건 | 0건 | — |
| Bounded Context | 6+1 (gateway) | 7+1 (gateway) | +1 (workspace) |
| Aggregate Root | 11개 | 12개 | +1 (Workspace) |
| Domain Event | 29종 | 31종 | +2 |
| Repository Port | 12개 | 13개 | +1 (WorkspaceRepository) |
| 종합 등급 | **A+** | **A+** | → 유지 |

### 주요 변화 분석

**추가된 항목**:
- **workspace 모듈 (NEW)**: Workspace Aggregate Root, 8 Command + 6 Query Use Case, WorkspaceRepository 포트, In-Memory 어댑터, Controller
- **TCC 인프라 (NEW)**: UnitOfWork 포트·구현체, CompensationStack, workflow-runtime Command TCC 적용
- **Outbox + DLQ (NEW)**: TransactionalEventPublisher, OutboxRelay, DeadLetterQueue (InMemory + TypeORM)
- **Optimistic Locking (NEW)**: TypeORM 리포지토리에 version 기반 락 적용

**유지된 항목**:
- 타입 안전성 (`as any` 0건, `: any` 0건)
- ESLint 클린 통과
- 도메인 레이어 순수성 (NestJS import 0건)
- 도메인 간 직접 import 위반 0건
- CQRS 패턴 엄격 준수
- 테스트 파일명 PascalCase/kebab-case 혼재 (27%/73%)

**악화된 항목**:
- 테스트/소스 비율 51.9% → 42.5% (코드 증가 대비 테스트 정체)

---

## 8. 우선순위별 개선 사항

### 🟡 단기 개선 (P1)

1. **workspace 모듈 테스트 작성**
   - Workspace Aggregate Root 도메인 단위 테스트
   - CreateWorkspace, DeleteWorkspace 등 8개 Command Use Case 테스트
   - ListWorkspaces, GetWorkspace 등 6개 Query 테스트
   - 테스트/소스 비율 회복 필요 (42.5% → 50%+ 목표)

2. **workspace TypeORM 어댑터 구현**
   - `WorkspaceRepository` TypeORM 구현체 작성
   - `WorkspaceChatHistory` TypeORM 구현체 작성
   - DB 모드에서 workspace 기능 동작 보장

### 🟢 중기 개선 (P2)

3. **테스트 파일명 컨벤션 통일**
   - PascalCase(18개) → kebab-case로 일관화 (소스 파일 컨벤션과 동일)
   - 003 리포트부터 7회 연속 지적 — 일괄 리네이밍 권장

4. **테스트 커버리지 측정 도입**
   - `npm run test:coverage` 정량적 추적
   - 테스트/소스 비율이 42.5%로 하락 — 모니터링 필요

5. **CI/CD 파이프라인 구성**
   - `typecheck → lint → test → build` 자동화
   - 아키텍처 규칙 위반 자동 검증 (architecture.test.ts CI 통합)

6. **bcrypt 네이티브 바인딩 검증**
   - auth 모듈의 bcrypt 의존성 배포 환경 호환성 확인
