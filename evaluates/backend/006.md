# 프로젝트 평가 리포트 — 2026.02.20

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | workflow-backend |
| 언어/런타임 | TypeScript ^5.9.3 / Node.js (ES2022, ESM) |
| 소스 파일 수 | 297개 (.ts) |
| 테스트 파일 수 | 41개 |
| 소스 코드 라인 | ~12,882줄 |
| 테스트 코드 라인 | ~5,303줄 |
| 외부 런타임 의존성 | @anthropic-ai/claude-agent-sdk, @nestjs/common, @nestjs/core, @nestjs/platform-express, @nestjs/typeorm, class-transformer, class-validator, pg, reflect-metadata, rxjs, typeorm, uuid |
| 아키텍처 | Hexagonal Architecture + DDD + Vertical Slice |

**요약**: AI 에이전트 기반 워크플로우 실행 관리 백엔드. 이전 리포트(005) 대비 소스 코드가 +2,101줄 증가하고 `mcp-gateway` 신규 모듈이 추가되었다. 타입 체크와 ESLint는 통과하나, 새 모듈의 아키텍처 규칙 위반과 Windows 경로 호환성 테스트 실패로 10개 테스트가 실패 중이다.

---

## 2. 아키텍처 평가

### 2.1 Vertical Slice 구조

```
src/
├── common/           (49 files, ~1,582 lines)  — 공유 ID, 이벤트, 포트, 에러, 유틸리티
├── workflow/         (40 files, ~1,960 lines)  — 워크플로우 정의/관리
├── workflow-runtime/ (93 files, ~5,140 lines)  — 워크플로우 실행/런타임
├── git/              (36 files, ~1,176 lines)  — Git 저장소 관리
├── mcp/              (31 files, ~797 lines)    — MCP 서버 관리
├── agent/            (38 files, ~1,400 lines)  — AI 에이전트 세션 관리
├── mcp-gateway/      (8 files, ~747 lines)     — MCP Gateway (신규) ⚠️
├── app.module.ts                               — NestJS 루트 모듈
└── main.ts                                     — 애플리케이션 엔트리포인트
```

| Feature | domain/ | application/ | infra/ | presentation/ | 상태 |
|---------|:---:|:---:|:---:|:---:|:---:|
| workflow | ✅ | ✅ | ✅ | ✅ | 완전 |
| workflow-runtime | ✅ | ✅ | ✅ | ✅ | 완전 |
| git | ✅ | ✅ | ✅ | ✅ | 완전 |
| mcp | ✅ | ✅ | ✅ | ✅ | 완전 |
| agent | ✅ | ✅ | ✅ | ✅ | 완전 |
| mcp-gateway | ❌ | ✅ | ❌ | ✅ | **불완전** |

**평가: A- (이전 A+ → 하락)**
- 5개 기존 Bounded Context는 완전한 수직 슬라이스 유지
- `mcp-gateway` 신규 모듈이 추가되었으나 Vertical Slice 구조를 따르지 않음 (domain/infra 레이어 없음)
- `mcp-gateway/application`에서 다른 feature의 domain/application을 직접 import하여 아키텍처 규칙 위반

### 2.2 DDD 적용 수준

| DDD 개념 | 적용 | 상세 |
|-----------|:---:|------|
| Aggregate Root | ✅ | 10개 엔티티 — Workflow, WorkflowRun, Git, McpServer, AgentSession, Report, WorkExecution, Checkpoint, WorkTree, WorkflowSpace. 모두 `AggregateRoot<TId>` 상속 |
| Value Object | ✅ | Branded Type + 팩토리 패턴 일관 적용 (IssueKey, GitUrl, BranchStrategy, CommitHash, SymLink 등 20종 이상) |
| Domain Event | ✅ | 30종 이상 이벤트, `BaseDomainEvent` 상속, `Object.freeze()` 페이로드 |
| Repository Port | ✅ | 10개 Repository 포트 — 모두 추상 클래스로 정의, In-Memory + TypeORM 어댑터 구현 완비 |
| Bounded Context | ⚠️ | 5개 기존 컨텍스트 명확 분리, 신규 `mcp-gateway`는 경계 위반 |
| Invariant 보호 | ✅ | private 생성자 + `create()`/`fromProps()` 팩토리 + 상태 전이 가드 |
| State Machine | ✅ | `RuntimeInvalidStateTransitionError`로 불법 전이 차단 |
| Error Hierarchy | ✅ | DomainError(isTransient) → ApplicationError 2단계 계층 |

**평가: A (이전 A+ → 하락)**
- 기존 10개 Aggregate Root는 DDD 패턴 100% 준수 유지
- `mcp-gateway`가 DDD 레이어링을 따르지 않아 Bounded Context 원칙 위반

### 2.3 헥사고날 아키텍처

| 모듈 | Port (도메인) | In-Memory Adapter | TypeORM Adapter | Controller | Module |
|------|:---:|:---:|:---:|:---:|:---:|
| workflow | ✅ | ✅ | ✅ | ✅ | ✅ |
| workflow-runtime | ✅ (7개+) | ✅ (7개+) | ✅ | ✅ | ✅ |
| git | ✅ | ✅ | ✅ | ✅ | ✅ |
| mcp | ✅ | ✅ | ✅ | ✅ | ✅ |
| agent | ✅ | ✅ | ✅ | ✅ | ✅ |
| common | ✅ (공유 포트 6개+, 유틸리티 3개) | ✅ | — | — | ✅ |
| mcp-gateway | ❌ | ❌ | ❌ | ✅ | ✅ |

**공유 유틸리티 추가** (`@common/utils/`):
- `tempBasePath()` — OS별 임시 경로 생성
- `buildPath()` — 플랫폼 독립적 경로 조합

**평가: A- (이전 A+ → 하락)**
- 기존 모듈들의 Port → Adapter 분리는 완전 유지
- `mcp-gateway`가 다른 feature의 Repository/UseCase를 직접 참조 — 헥사고날 원칙 위반
- `@common/utils/` 추가로 공통 유틸리티 정비 진행 중

---

## 3. 코드 품질 검증

### 3.1 TypeScript 타입 체크

```
$ npm run typecheck
→ ✅ PASS (0 errors)
```

strict 모드에서 에러 없음. **우수.**

### 3.2 ESLint 검사

```
$ npm run lint
→ ✅ PASS (0 errors, 0 warnings)
```

005 리포트에서 지적된 `claude-agent-client.ts`의 미사용 변수 에러가 **해소**되었다.

### 3.3 테스트 실행

```
$ npm run test
→ ❌ 7 test files failed, 34 passed (41 files)
   10 tests failed, 241 passed, 14 skipped (265 tests)
   Duration: ~20s
```

| 실패 유형 | 파일 | 실패 테스트 수 | 원인 |
|----------|------|:-------------:|------|
| 아키텍처 규칙 위반 | `architecture.test.ts` | 1 | `mcp-gateway/application`이 다른 feature를 직접 import |
| Windows 경로 호환성 | `workspace-path-factory.test.ts` | 4 | 테스트가 Unix 스타일 경로(`/tmp/spaces`)를 기대하나 Windows에서는 역슬래시 생성 |
| 이벤트 발행 시점 | `create-git-use-case.test.ts` | 1 | 이벤트 발행 로직 변경으로 인한 기대값 불일치 |
| 이벤트 발행 시점 | `delete-git-use-case.test.ts` | 1 | 이벤트 발행 로직 변경으로 인한 기대값 불일치 |
| WorkflowSpace 생성 | `start-workflow-run-use-case.test.ts` | 2 | Mock 기대값 불일치 |
| WorkExecution 시작 | `StartNextWorkExecutionUseCase.test.ts` | 1 | Mock 기대값 불일치 |

**테스트 계층별 현황**:

| 테스트 계층 | 파일 수 | 통과/전체 | 상태 |
|-------------|:-------:|:---------:|:---:|
| 아키텍처 검증 | 1 | 10/11 | ⚠️ |
| 단위 - 도메인 | 12 | 97/97 | ✅ |
| 단위 - 애플리케이션 | 23 | 63/74 | ⚠️ |
| 단위 - 인프라 | 1 | 28/28 | ✅ |
| 통합 | 4 | 43/55 | ⚠️ |

---

## 4. 상세 문제 분석

### 🔴 Critical — 아키텍처 규칙 위반 (mcp-gateway)

`mcp-gateway/application/tool-registrars/` 파일들이 다른 feature의 domain/application을 직접 import:

| 파일 | 위반 import |
|------|-------------|
| `git-tools.ts:4-6` | `@git/domain/ports/git-repository.js`, `@git/application/use-cases/*` |
| `mcp-server-tools.ts:4-7` | `@mcp/domain/ports/mcp-server-repository.js`, `@mcp/application/use-cases/*` |
| `workflow-tools.ts:4-14` | `@workflow/domain/ports/*`, `@workflow/application/use-cases/*`, `@workflow/domain/value-objects/*` |
| `workflow-run-tools.ts:4-13` | `@workflow-runtime/domain/ports/*`, `@workflow-runtime/application/use-cases/*` |

이는 CLAUDE.md §1 "도메인 간 직접 import 금지" 규칙을 명백히 위반한다. `mcp-gateway`는 현재 아키텍처 테스트(`architecture.test.ts`)에 등록되지 않은 feature로 검출되어 `null application`으로 표시된다.

**권장 해결 방안**:
1. `mcp-gateway`를 FEATURES 목록에 추가하고 의존 관계 정의
2. 또는 `mcp-gateway`를 별도의 "orchestration layer"로 분리하여 Composition Root 역할 부여
3. 현재 구조가 의도된 것이라면 아키텍처 테스트 규칙 수정 필요

### 🟡 Warning — Windows 경로 호환성 테스트 실패

`workspace-path-factory.test.ts`의 4개 테스트가 Windows 환경에서 실패:

| 테스트 | Expected | Received |
|--------|----------|----------|
| workflowSpacePath | `/tmp/spaces/run-001` | `\tmp\spaces\run-001` |
| workTreePath | `/tmp/spaces/run-001/work-trees/git-001` | `\tmp\spaces\run-001\work-trees\git-001` |
| workSpacePath | `/tmp/spaces/run-001/workspaces/we-001` | `\tmp\spaces\run-001\workspaces\we-001` |
| default base | `/tmp/workflow-spaces/run-001` | `C:\Users\...\Temp\workflow-spaces\run-001` |

구현체(`WorkspacePathFactory`)는 `@common/utils/buildPath`를 사용하여 플랫폼 독립적 경로를 생성하나, 테스트는 Unix 스타일 경로를 하드코딩하고 있다.

### 🟡 Warning — UseCase 테스트 Mock 불일치

`create-git-use-case.test.ts`, `delete-git-use-case.test.ts`, `start-workflow-run-use-case.test.ts`, `StartNextWorkExecutionUseCase.test.ts`에서 이벤트 발행 로직 또는 메서드 호출 순서 변경으로 인한 Mock 기대값 불일치가 발생.

### 🟢 Info — 테스트 파일명 컨벤션 혼재

| 컨벤션 | 파일 수 | 비율 | 예시 |
|--------|:-------:|:----:|------|
| PascalCase | 18 | 44% | `AgentSession.test.ts`, `Workflow.test.ts` |
| kebab-case | 23 | 56% | `create-git-use-case.test.ts`, `git.test.ts` |

003 리포트부터 지적된 동일 이슈 — 일관화 권장.

---

## 5. 강점

### 5.1 기존 5개 Bounded Context의 완전한 구조 유지

workflow, workflow-runtime, git, mcp, agent 모듈은 여전히 `domain/application/infra/presentation` 4계층을 완비하고 있으며, 10개 Repository + 6개 이상 Query Adapter에 대해 In-Memory + TypeORM 이중 어댑터 구현이 완전하다.

### 5.2 도메인 레이어 순수성 유지 (기존 모듈)

기존 5개 feature의 도메인 레이어(`**/domain/**/*.ts`)에서:
- NestJS import: **0건**
- `@Injectable` 데코레이터: **0건**
- `as any`: **0건**
- 다른 feature 직접 import: **0건**

### 5.3 ESLint 에러 해소

005 리포트에서 지적된 `claude-agent-client.ts:125`의 미사용 변수 에러가 수정되어 ESLint가 완전 통과한다.

### 5.4 공통 유틸리티 추가

`@common/utils/` 디렉토리가 추가되어 플랫폼 독립적 경로 처리 유틸리티(`tempBasePath`, `buildPath`)가 정비되었다. 이는 크로스 플랫폼 호환성 개선을 위한 기반 작업이다.

### 5.5 코드 규모 성장

005 대비 소스 코드 +2,101줄(+19%) 증가하며 `mcp-gateway` 모듈(~747줄) 신규 추가. 활발한 기능 개발이 진행 중이다.

---

## 6. 종합 평가

| 영역 | 점수 | 이전(005) | 변화 | 비고 |
|------|:----:|:---------:|:----:|------|
| **아키텍처 설계** | B+ | A+ | ↓↓ | mcp-gateway 도메인 간 위반 |
| **도메인 모델링** | A | A+ | ↓ | 기존 모듈 유지, 신규 모듈 미적용 |
| **타입 안전성** | A+ | A+ | → | `as any` 0건 유지 |
| **코드 품질** | A+ | A | ↑ | ESLint 에러 0건 달성 |
| **테스트** | B+ | A+ | ↓↓ | 10/265 실패 (아키텍처 + 경로 호환성) |
| **완성도** | B | A+ | ↓↓ | 테스트 실패 상태, 미커밋 변경 다수 |
| **코드 컨벤션** | A- | A- | → | 테스트 파일명 혼재 잔존 |

### 종합 등급: **B+**

이전 리포트(005)의 A+에서 **2단계 하락**. 주요 원인:

1. **아키텍처 규칙 위반**: `mcp-gateway` 모듈이 다른 feature의 domain/application을 직접 import하여 CLAUDE.md §1 위반. 아키텍처 테스트 실패.
2. **테스트 실패**: 10개 테스트 실패 (아키텍처 위반 1, 경로 호환성 4, Mock 불일치 5)
3. **미커밋 변경**: git status에 다수 변경 파일 존재

긍정적 측면으로 ESLint 에러 0건 달성, 기존 5개 모듈의 DDD/헥사고날 패턴 유지, 공통 유틸리티 정비 진행 등이 있다.

---

## 7. 이전 리포트와 비교

| 지표 | 005 (이전) | 006 (현재) | 변화 |
|------|:----------:|:----------:|:----:|
| 소스 파일 수 | 270개 | 297개 | +27 (+10%) |
| 테스트 파일 수 | 41개 | 41개 | — |
| 소스 코드 라인 | ~10,781줄 | ~12,882줄 | +2,101 (+19%) |
| 테스트 코드 라인 | ~5,196줄 | ~5,303줄 | +107 (+2%) |
| 타입 체크 | ✅ PASS | ✅ PASS | — |
| ESLint | 1 error | **0 errors** | ✅ 개선 |
| 테스트 결과 | 260/260 통과 | **251/265 (10 실패)** | ❌ 악화 |
| `as any` 사용 | 0건 | 0건 | — |
| 도메인 간 위반 (domain/app) | 0건 | **4건 (mcp-gateway)** | ❌ 신규 |
| Bounded Context | 5개 | 6개 (+mcp-gateway) | +1 |
| 종합 등급 | **A+** | **B+** | ↓ 2단계 |

### 주요 변화 분석

**개선된 항목**:
- **ESLint 완전 통과**: 005의 미사용 변수 에러 1건 해소
- **공통 유틸리티 추가**: `@common/utils/`에 플랫폼 독립적 경로 처리 함수 추가
- **신규 기능 개발**: `mcp-gateway` 모듈 ~747줄 추가

**악화된 항목**:
- **아키텍처 규칙 위반**: `mcp-gateway`가 4개 feature의 domain/application을 직접 import
- **테스트 실패**: 260/260 → 251/265 (10개 실패)
- **미커밋 변경**: git status에 다수 파일 변경 존재

**유지된 항목**:
- 기존 5개 모듈의 도메인 순수성 (NestJS import 0건, `as any` 0건)
- 테스트 파일명 PascalCase/kebab-case 혼재

---

## 8. 우선순위별 개선 사항

### 🔴 즉시 수정 (P0)

1. **mcp-gateway 아키텍처 규칙 준수**
   - 현재 `mcp-gateway/application`이 다른 feature의 domain/application을 직접 import
   - 해결 방안 A: `mcp-gateway`를 Vertical Slice 구조로 정규화 (domain/ports 정의, adapter 구현)
   - 해결 방안 B: `mcp-gateway`를 "orchestration layer"로 정의하고 아키텍처 테스트 규칙 수정
   - 해결 방안 C: 공유 포트를 `@common/ports/`에 정의하고 Query Adapter 패턴 적용

2. **UseCase 테스트 Mock 불일치 수정**
   - `create-git-use-case.test.ts`, `delete-git-use-case.test.ts` 등 5개 테스트의 Mock 기대값을 실제 구현에 맞게 수정

### 🟡 단기 개선 (P1)

3. **Windows 경로 호환성 테스트 수정**
   - `workspace-path-factory.test.ts`의 테스트가 Unix 스타일 경로를 하드코딩
   - `path.join()` 결과와 비교하도록 수정하거나, 정규화된 경로 비교 유틸리티 사용

4. **미커밋 변경 정리**
   - 현재 git status에 다수 변경 파일 존재
   - 작업 완료 후 적절한 단위로 커밋 필요

### 🟢 중기 개선 (P2)

5. **테스트 파일명 컨벤션 통일**
   - PascalCase(18개) → kebab-case로 일관화 (소스 파일 컨벤션과 동일)
   - 003 리포트부터 지적 — 일괄 리네이밍 권장

6. **테스트 커버리지 측정 도입**
   - `npm run test:coverage` 설정은 이미 존재
   - 정량적 커버리지 추적 및 목표 설정

7. **CI/CD 파이프라인 구성**
   - `typecheck → lint → test → build` 자동화
   - 아키텍처 규칙 위반 자동 검증 (이미 architecture.test.ts 존재 — CI에 통합만 필요)
